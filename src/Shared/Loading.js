import React from "react";

const Loading = () => {
  return (
    <div className="h-screen flex justify-center items-center">
      <button className="btn btn-square btn-primary loading" />
    </div>
  );
};

export default Loading;
